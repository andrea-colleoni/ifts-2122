SELECT 
		1, -- valore (literal) intero
		(2 * 4) / 3 + 15, -- espressione
		'pippo', -- valore di tipo char
		16.5, -- valore decimal
		`titolo`, -- ` (backtick) ALT + 96
		titolo,
		`numero stelle gradimento`
FROM
	`corso`